<?xml version="1.0" encoding="UTF-8"?>
<!--
  Test logging. The main logback-spring.xml only attaches appenders for the dev/prod profiles,
  so under @ActiveProfiles("test") the application would otherwise log nothing. This gives test
  runs (local and CI) readable console output for diagnosing failures.
-->
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%X{traceId}] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <logger name="com.inventory" level="DEBUG"/>
</configuration>
